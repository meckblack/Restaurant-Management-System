@model IEnumerable<FMS_Objects.Enities.ExpenseCategory>
@{
    ViewBag.Title = "Index";
}

<style>
    .modal-backdrop {
        z-index: 0;
    }
    div#myModal.modal.fade.in {
        overflow: hidden;
    }
</style>
<div class="container-fluid">
    <div class="row">
        <div class="col-md-12">
            <ul class="breadcrumb">
                <li><a href="@Url.Action("Index", "Dashboard")">Dashboard</a></li>
                <li><a href="@Url.Action("Index", "Restaurant")">Restaurant</a></li>
                <li><a href="@Url.Action("Index", "RestaurantDashboard", new { id = Convert.ToInt32(Session["recID"]) })"> @Session["recNamee"].ToString() </a></li>
                <li>Expense Category</li>
                
                    <a href="@Url.Action("Create", "ExpenseCategory")" data-modal="" class="btn btn-primary pull-right">ADD</a>
                
            </ul>
            
            <div class="card">
                <div class="card-header" data-background-color="purple">
                    <h4 class="title">EXPENSE CATEGORIES</h4>
                </div>
                <div class="card-content table-responsive">
                    @using (Html.BeginForm())
                    {
                        <table class="table">
                            <thead class="text-primary">
                                <tr>
                                    <th>
                                        @Html.DisplayNameFor(model => model.ExpenseCategoryTitle)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.ExpenseCategoryDescription)
                                    </th>
                                    <th>
                                        Actions
                                    </th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.ExpenseCategoryTitle)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.ExpenseCategoryDescription)
                                        </td>
                                        <td>
                                            <a href="@Url.Action("Index", "ExpenseItem", new { id = item.ExpenseCategoryId })"><i style="padding-right: 5px" class="fa fa-2x fa-share-square-o"></i></a>
                                            <a href="@Url.Action("Edit", "ExpenseCategory", new { id = item.ExpenseCategoryId })" data-modal=""><i style="padding-right: 5px" class="fa fa-2x fa-edit"></i></a>
                                            <a href="@Url.Action("Delete", "ExpenseCategory", new { id = item.ExpenseCategoryId })" data-modal=""><i class="fa fa-2x fa-trash"></i></a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
<!-- modal placeholder-->
<div id='myModal' class='modal fade in'>
    <div class="modal-dialog">
        <div class="modal-content">
            <div id='myModalContent'>
            </div>
        </div>
    </div>
</div>
<script src="~/scripts/jquery-1.10.2.min.js" type="text/javascript"></script>
<script src="~/Scripts/Appjs/FMS.js"></script>
